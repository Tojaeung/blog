import styled from 'styled-components';
import { CommonButtonStyle } from 'styles/common';

export const Container = styled.div`
  width: 100%;
  margin: 0 auto;
  display: flex;
  gap: 20px;
`;
export const GoCategory = styled(CommonButtonStyle)``;
export const GoPost = styled(CommonButtonStyle)``;
